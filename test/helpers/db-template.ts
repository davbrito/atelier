/**
 * Name of the migrated template database (see test/setup.ts), which each
 * test file clones into its own uniquely-named database (see
 * test/helpers/fixtures.ts) instead of every file sharing one database.
 */
export const TEMPLATE_DATABASE = "migrated_template";
